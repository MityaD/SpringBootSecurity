spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    password: yakushik
    username: yakushik
    url: jdbc:postgresql://localhost:5432/yakushik_db
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    generate-ddl: true
    database: postgresql